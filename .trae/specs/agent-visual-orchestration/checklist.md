- [x] `Agent` 数据库表成功增加了 `workflow` (JSON) 字段并同步。
- [x] 后端 `agents` 的 POST/PUT 接口成功接收并保存了 `workflow` 数据。
- [x] 前端成功引入 `@xyflow/react` 依赖且项目无构建报错。
- [x] 智能体编辑页 (Agent Editor) 从原本纯表单变更为具有左、中（画布）、右三栏的可视化拖拽编排界面。
- [x] 用户可以从左侧组件库将不同的节点（如大模型、工具、控制逻辑）拖拽入画布中，并可以将其连线。
- [x] 点击画布上的任意节点，右侧属性面板能够正确展示该节点的配置表单（如：修改节点的 Prompt 属性），并且这些修改能够实时反映到节点的 `data` 状态中。
- [x] 点击“保存”时，画布上的节点 (`nodes`) 和连线 (`edges`) 被成功序列化为 JSON 存入数据库，并在重新打开页面时能精确还原。