- [ ] 左下角的原生放大缩小控件已经移动到了画布正底部的居中位置。
- [ ] 底部控制栏中新增了两个图标按钮：代表自动布局的 `Layout` 图标，和代表下载图片的 `Download` 图标。
- [ ] 当画布中有凌乱的节点连线时，点击“自动布局”按钮，节点能够按照层级（如从上到下）自动排布整齐，连线的锚点也正确指向（如上进下出），且画布会自动居中适应屏幕。
- [ ] 点击“下载图片”按钮后，浏览器会弹出一个 `workflow.png` 文件的下载，且打开图片后能清晰地看到所有流程节点的完整内容（不会被屏幕视口裁剪）。