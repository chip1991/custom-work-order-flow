- [ ] 流程条件节点的 `conditionConfig` 数据结构已重构为 `{ logicalOperator: 'AND' | 'OR', conditions: Array }` 格式，支持多条组合。
- [ ] 属性面板（`PropertiesPanel.tsx`）中条件判断节点的“判断字段”由手动输入框改为了下拉选择框，其选项来源于当前流程的 `formConfig` 表单配置字段。
- [ ] 属性面板中，当选择不同类型的表单字段（例如 `number` 或 `select`）时，其对应的操作符列表（如 `>`, `<`, `==`）会根据字段类型动态变化。
- [ ] 属性面板中，当选择不同类型的表单字段时，其“比较值”控件会动态变化（如文本框变为数字框，或下拉框直接选择预设 `options`）。
- [ ] 属性面板中，支持点击“+ 添加条件”增加多行规则，支持通过垃圾桶图标删除单行规则，并能切换“所有条件(AND)/任一条件(OR)”逻辑。
- [ ] 画布节点（`ConditionNode.tsx`）上能正确根据当前 `conditionConfig` 中的规则数量（0条、1条、多条）展示对应的精简摘要文本。