- [ ] 后端更新接口（PUT /api/processes/:id）在收到空值、非数字、或非字符串的 `timeLimit` 和 `communities` 时，不会再导致 Prisma 报错（如 `NaN`）。
- [ ] 适用小区被替换成了复选框列表（例如：朝阳、海淀、望京小区等），用户可以多选，并且正确回显。
- [ ] 处理时效拆分成了“天”、“小时”、“分钟”三个独立的数字输入框，用户修改任意一个，底层的总分钟数 `timeLimit` 会自动重新计算并存储。
- [ ] 启用状态的下拉列表中包含“草稿”这个选项。
- [ ] 整体编辑流程（包括点击“保存”按钮）能够成功提交并跳转，不再出现“Failed to update process”的红色报错提示。