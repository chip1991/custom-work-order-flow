
---

时间：$(date "+%H:%M:%S")
User:
由于之前的工单代码被错误地写在了 backend 和 frontend 目录中，而当前实际运行的是 apps/api 和 apps/web，你需要将 WorkOrderFlow 的全栈功能迁移到正确的目录中：
1. `apps/api/prisma/schema.prisma` 中增加 `WorkOrderFlow`，执行 prisma generate/migrate。
2. `apps/api/src/routes/workOrders.ts` 编写 CRUD，并在 `apps/api/src/app.ts` 或 `index.ts` 挂载路由。
3. 把 `frontend/src/pages/WorkOrders` 拷贝到 `apps/web/src/pages/WorkOrders`，并调整依赖（例如 UI 组件）。
4. 在 `apps/web/src/layout/AppLayout.tsx` 的侧边栏加入入口。
5. 在 `apps/web/src/App.tsx` 加入路由。
6. 构建验证。

AI:
我已经完成了将 WorkOrderFlow 的全栈功能从 backend 和 frontend 迁移到 apps/api 和 apps/web 目录的工作：
1. 在 `apps/api/prisma/schema.prisma` 中增加了 `WorkOrderFlow` 模型，并执行了 `prisma generate` 和 `prisma migrate dev`。
2. 编写了 `apps/api/src/routes/workOrders.ts` 的 CRUD 路由，并在 `apps/api/src/app.ts` 成功挂载。
3. 将 `frontend/src/pages/WorkOrders` 和相关的 API 拷贝到了 `apps/web/src/pages/WorkOrders` 和 `apps/web/src/api/work-orders.ts` 中，调整了依赖，并补充安装了缺失的 `lucide-react` 和 `@xyflow/react` 依赖。
4. 在 `apps/web/src/layout/AppLayout.tsx` 侧边栏加入了“工单管理”入口。
5. 在 `apps/web/src/App.tsx` 中加入了相关的页面路由。
6. 运行了前后端的 `npm run build` 构建，验证通过，并启动了 `npm run dev` 前后端服务，前端服务预览地址已为您准备好。
