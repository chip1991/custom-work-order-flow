## 2026-04-18 15:53:21\n\n**User:** Implement Task 3 for adding a Database Executor.\nTask 3: In `backend`, install `mysql2` and `pg`. In `backend/services/dag-engine.js`, add `executeDatabase` method. It should securely execute parameterized queries based on node configuration (`dbType`, `connectionString`, `sql`, `params`) using `contextManager.substituteObject(node.data)`. Wait for connection, execute query with `params` array to prevent injection, and set the result rows to `this.contextManager.set(node.id, { output: rows })`. Emit SSE events.\nUpdate tasks.md for Task 3.\n\nRespond in Chinese.\n\n
